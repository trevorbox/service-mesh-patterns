apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "redis-cli.fullname" . }}-test-connection"
  labels:
    {{- include "redis-cli.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "redis-cli.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
