apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: oracledb
  namespace: istio-egress
spec:
  selector:
    istio: egressgateway
  servers:
  - port:
      number: 443
      name: tls
      protocol: TLS
    hosts:
    - {{ .Values.oracledb.host }}
    tls:
      mode: ISTIO_MUTUAL
