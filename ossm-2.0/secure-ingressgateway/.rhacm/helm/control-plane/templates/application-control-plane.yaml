---
apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: control-plane-latest
spec:
  type: GitHub
  pathname: https://github.com/trevorbox/service-mesh-patterns.git
---
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: control-plane
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - control-plane
---
apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: control-plane
  labels:
    app: control-plane
  annotations:
    apps.open-cluster-management.io/github-path: ossm-2.0/secure-ingressgateway/helm/control-plane
    apps.open-cluster-management.io/github-branch: rhacm
spec:
  channel: {{ .Release.Namespace }}/control-plane-latest
  placement:
    placementRef:
      kind: PlacementRule
      name: dev-clusters

