---
apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: service-mesh-operators-latest
spec:
  type: GitHub
  pathname: https://github.com/trevorbox/service-mesh-patterns.git
---
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: service-mesh-operators
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - service-mesh-operators
---
apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: service-mesh-operators
  labels:
    app: service-mesh-operators
  annotations:
    apps.open-cluster-management.io/github-path: ossm-2.0/secure-ingressgateway/helm/service-mesh-operators
    apps.open-cluster-management.io/github-branch: rhacm
spec:
  channel: {{ .Release.Namespace }}/service-mesh-operators-latest
  placement:
    placementRef:
      kind: PlacementRule
      name: dev-clusters

