---
apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: operators-latest
spec:
  type: GitHub
  pathname: https://github.com/trevorbox/service-mesh-patterns.git
---
apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: operators
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - operators
---
apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: operators
  labels:
    app: operators
  annotations:
    apps.open-cluster-management.io/github-path: ossm-2.0/secure-ingressgateway/helm/operators
    apps.open-cluster-management.io/github-branch: rhacm
spec:
  channel: {{ .Release.Namespace }}/operators-latest
  placement:
    placementRef:
      kind: PlacementRule
      name: dev-clusters

