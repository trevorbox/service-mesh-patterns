kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: console
spec:
  to:
    kind: Service
    name: minio-dev
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
