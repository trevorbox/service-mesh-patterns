apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oracledb-free.fullname" . }}-test-connection"
  labels:
    {{- include "oracledb-free.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oracledb-free.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
