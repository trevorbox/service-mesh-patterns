apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dbeaver.fullname" . }}-test-connection"
  labels:
    {{- include "dbeaver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dbeaver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
