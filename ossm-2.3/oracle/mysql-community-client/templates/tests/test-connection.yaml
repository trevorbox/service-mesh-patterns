apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mysql-community-client.fullname" . }}-test-connection"
  labels:
    {{- include "mysql-community-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mysql-community-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
